#!csharp

using Microsoft.DotNet.Interactive;
using Extension;

if (KernelInvocationContext.Current is { } currentContext)
{
    currentContext.DisplayAs("Hello world! Extension.dib loaded!", "text/markdown");
}
